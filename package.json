{
  "name": "usat-telegram-bot",
  "version": "1.0.0",
  "description": "University suggestion and complaint bot",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "dev": "nodemon --ignore data/ --ignore logs/ --ignore *.log bot.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "node-telegram-bot-api": "^0.66.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
